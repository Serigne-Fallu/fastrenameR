% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_metadata.r
\name{read_metadata_tsv}
\alias{read_metadata_tsv}
\title{Read metadata and build a renaming mapping}
\usage{
read_metadata_tsv(path, id_col, out_cols, delim = "\\t")
}
\arguments{
\item{path}{Path to the metadata file.}

\item{id_col}{ID column: character (name) or integer (1‑based index).}

\item{out_cols}{Output columns: vector of names or 1‑based indices.}

\item{delim}{Field delimiter (default: tab).}
}
\value{
A list with elements \code{mapping} (named character vector),
\code{raw_data} (tibble), \code{id_col_name}, and \code{out_col_names}.
}
\description{
Reads a TSV (or CSV with given delimiter), identifies an ID column and a set
of output columns, then creates a named vector that maps each sequence ID to
a new header string formed by joining the cleaned output column values with "_".
}
