% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rename_fasta.r
\name{rename_fasta}
\alias{rename_fasta}
\title{Rename FASTA headers using a metadata mapping}
\usage{
rename_fasta(fasta_in, metadata_map, fasta_out = NULL, verbose = TRUE)
}
\arguments{
\item{fasta_in}{Path to input FASTA (.gz allowed).}

\item{metadata_map}{Named character vector: names are old IDs, values new names.}

\item{fasta_out}{Output file; if NULL, the renamed FASTA is returned as a
character vector (invisibly).}

\item{verbose}{If TRUE, print renaming progress.}
}
\value{
If \code{fasta_out} is provided, the output path (invisibly);
otherwise the renamed FASTA lines.
}
\description{
Reads a FASTA file (optionally .gz) and replaces each header with the
corresponding new name from the mapping. Sequences without a match are
kept unchanged and a warning is emitted.
}
