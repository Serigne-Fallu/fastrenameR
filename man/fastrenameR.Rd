% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastrenameR.r
\name{fastrenameR}
\alias{fastrenameR}
\title{Fast FASTA header renaming using metadata}
\usage{
fastrenameR(
  metadata,
  fasta,
  output = NULL,
  id_col = NULL,
  out_cols = NULL,
  delim = "\\t",
  verbose = TRUE,
  show_cols = FALSE,
  standardize_dates = FALSE,
  date_mid_month_day = 15,
  date_mid_year_day = 182
)
}
\arguments{
\item{metadata}{Path to metadata TSV file.}

\item{fasta}{Path to input FASTA (.gz allowed).}

\item{output}{Output FASTA file. If NULL, uses input name + "_renamed.fasta".}

\item{id_col}{ID column: name or 1-based index from the metadata header.}

\item{out_cols}{Output columns: vector of names or indices. These will be
joined with "_" to form the new FASTA header.}

\item{delim}{Metadata delimiter (default: tab).}

\item{verbose}{Print progress messages.}

\item{show_cols}{If TRUE, display available column names before renaming.}

\item{standardize_dates}{Logical, whether to standardize date formats in output.}

\item{date_mid_month_day}{Day for month-year formats (default: 15).}

\item{date_mid_year_day}{Day for year-only formats (default: 182, July 1).}
}
\value{
The output file path (invisibly).
}
\description{
Reads a metadata TSV, lets the user specify which columns to use for the new
FASTA header names based on the metadata header, and renames the input FASTA.
}
\examples{
\dontrun{
# Show available columns first
fastrenameR::show_columns("metadata.tsv")

# Rename with country + date
fastrenameR("metadata.tsv", "seqs.fasta",
            id_col = "accessionVersion",
            out_cols = c("geoLocCountry", "sampleCollectionDate"))

# Rename with date standardization
fastrenameR("metadata.tsv", "seqs.fasta",
            id_col = "accessionVersion",
            out_cols = c("geoLocCountry", "sampleCollectionDate"),
            standardize_dates = TRUE)
}
}
