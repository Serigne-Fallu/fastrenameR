## Test environments
* local Windows 11, R 4.6.0
* GitHub Actions (ubuntu-latest, macos-latest, windows-latest)

## R CMD check results
0 errors | 0 warnings | 1 note

* This is a new submission.

## Reverse dependencies
This is a new package, so there are no reverse dependencies.

## Comments
This package provides fast FASTA header renaming with metadata and 
date standardization for phylogenetic workflows (BEAST/BEAUti).

## CRAN Pre-submission
The package was checked with:
- devtools::check() -> 0 errors, 0 warnings, 1 note (new submission)
- devtools::check_win_devel() -> OK
- devtools::check_win_release() -> OK